server:
  servlet:
    context-path: /api/v1

# spring
spring:
  application:
    name: nntask

# Other
wallets:
  supported-currencies: ${SUPPORTED_CURRENCIES:PLN,USD}
conversion:
  base-currency: ${BASE_CURRENCY:PLN}